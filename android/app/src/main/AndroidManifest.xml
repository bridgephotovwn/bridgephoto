<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- No permissions. The camera is operated by the Google Play services
         document-scanner activity. Whether the app may use the network is
         decided per edition, in src/free and src/business: the public app
         removes INTERNET, the company one asks for it. -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <!--
        For the four-shot capture that reads the shape of a page - an embossed
        seal, a stamped plate, the dent a biro leaves. It needs the torch held
        on across four frames, which the ML Kit scanner screen does not allow,
        so those shots are taken by this app itself.

        This is the ONLY permission BRIDGE PHOTO asks for. Not storage, and
        above all not INTERNET - the release build reads its own merged
        manifest and refuses to finish if INTERNET ever appears in it.
    -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
        The camera plugin brings a MICROPHONE and a STORAGE permission with it,
        because it can also record video. This app cannot, and a scanner that
        asks for the microphone has given away the only thing that makes it
        worth trusting. Both are struck out of the merged manifest here.

        The Gradle check that guards INTERNET guards these two as well, so no
        future library can put them back quietly.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <application
        android:label="BRIDGE PHOTO"
        android:allowBackup="false"
        android:largeHeap="true"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Ask Google Play services to fetch the OCR models at install time. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr,ocr_devanagari" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
